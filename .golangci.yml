linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
  # revive intentionally omitted: its default unused-parameter rule is noisy
  # for interface-mandated params (cobra cmd/args, injected callback ctx).
  # govet + staticcheck cover the real defects.
